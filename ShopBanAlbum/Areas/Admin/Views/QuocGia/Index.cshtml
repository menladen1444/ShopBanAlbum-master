
@{
    ViewBag.Title = "Index";
}
@section Scripts{
    <!-- Page level plugins -->
    <script src="/Assets/Admin/vendor/datatables/jquery.dataTables.min.js"></script>
    <script src="/Assets/Admin/vendor/datatables/dataTables.bootstrap4.min.js"></script>

    <!-- Page level custom scripts -->
    <script src="/Assets/Admin/js/demo/datatables-demo.js"></script>
}
<!-- Begin Page Content -->
<div class="container-fluid">

    <!-- Page Heading -->
    <div class="d-flex justify-content-between pb-2">
        <h1 class="h3 mb-2 text-gray-800">Danh sách Quốc gia</h1>
        <div>
            <a type="submit" class="btn btn-success" href="@Url.Action("Create","QuocGia")">
                <i class="fas fa-plus"></i>
                <span>Thêm Quốc gia</span>
            </a>
        </div>
    </div>
    <!-- DataTales Example -->
    <div class="card shadow mb-4">

        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered" id="quocGiaDataTable" width="100%" cellspacing="0">
                    <thead>
                        <tr>
                            <th>QuocGiaID</th>
                            <th>Tên quốc gia</th>
                            <th width="10%">Edit</th>
                            <th width="10%">Delete</th>
                        </tr>
                    </thead>
                    <tfoot>
                        <tr>
                            <th>QuocGiaID</th>
                            <th>Tên quốc gia</th>
                            <th>Edit</th>
                            <th>Delete</th>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>

</div>
<!-- /.container-fluid -->
<script>
function DeleteData(QuocGiaID) {
    if (confirm("Bạn chắc chắn muốn xóa Quốc gia này ...?")) {
        Delete(QuocGiaID);
    }
    else {
        return false;
    }
}

    function Delete(QuocGiaID) {
var url = '@Url.Content("/QuocGia/DeleteQuocGia")';
        $.post(url, { ID: QuocGiaID }, function (data) {
        if (data == "Deleted") {
            alert("Xóa thành công!");
            oTable = $('#quocGiaDataTable').DataTable();
            oTable.draw();
        }
        else {
            alert("Something Went Wrong!");
        }
    });
}
</script>
